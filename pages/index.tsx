import React from "react";
import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div
      style={{
        display: "grid",
        placeItems: "center",
        height: "100vh",
        fontFamily: "Alaska, sans-serif",
        fontSize: "5rem",
        fontWeight: "900",
      }}>
      Coming Soon
    </div>
  );
};

export default Home;
