import Projects from "./(components)/Projects";

const ProjectsPage = () => {
  return (
    <div
      style={{
        overflow: "hidden",
        height: "100%",
        marginLeft: "1rem",
      }}>
      <Projects />
    </div>
  );
};

export default ProjectsPage;
